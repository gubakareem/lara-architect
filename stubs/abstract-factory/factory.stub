<?php

namespace {{ namespace }};

/**
 * Abstract Factory — creates a family of related {{ model }} components
 * (notifier + serializer) without coupling to concrete classes.
 */
interface {{ model }}ComponentFactory
{
    public function createNotifier(): {{ model }}Notifier;

    public function createSerializer(): {{ model }}Serializer;
}
