<?php

namespace {{ namespace }};

use {{ repositoryClass }};
use KarimAshraf\LaraArchitect\Services\ArchitectService;

/**
 * @extends ArchitectService<{{ model }}>
 */
class {{ model }}Service extends ArchitectService
{
    public function __construct({{ model }}Repository $repository)
    {
        parent::__construct($repository);
    }

    // Override prepareForCreate(), prepareForUpdate(), created(), updated()
    // or deleted() to attach {{ model }}-specific business logic.
}
